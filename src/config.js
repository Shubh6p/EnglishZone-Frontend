// ==========================================
// ENGLISH ZONE - FRONTEND CONFIGURATION
// ==========================================

// Change this URL to point to your live backend server.
// If you are developing locally, you can change it back to 'http://localhost:5000/api/v1'

const config = {
    // Current Live Vercel Backend URL
    BACKEND_DOMAIN: 'https://english-zone-backend.vercel.app',
    
    // Set to false in production if you want users to type their passwords manually
    DEMO_MODE: true
};

export default config;
